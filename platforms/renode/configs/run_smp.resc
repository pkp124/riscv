# Renode launch script for RISC-V bare-metal SMP application (4 harts)
# ===================================================================
# Usage: renode -e "set elf @path/to/app.elf; s @platforms/renode/configs/run_smp.resc"

mach create
machine LoadPlatformDescription @platforms/renode/riscv_virt_smp.repl

sysbus LoadELF $elf

sysbus.uart0 CreateFileBackend "uart_output.txt" false

start
emulation runFor "00:00:05"
quit
